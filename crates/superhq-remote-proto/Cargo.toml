[package]
name = "superhq-remote-proto"
version = "0.1.0"
edition = "2024"
description = "Wire protocol types for SuperHQ remote control (JSON-RPC 2.0 over iroh)"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
thiserror = "1"
